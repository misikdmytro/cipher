[log]
min_level = "DEBUG"

[database]
username = "cipher"
password = "cipher"
host = "localhost"
port = 5432
database = "scheduler"
use_ssl = false

[grpc]
scheme = "http"
host = "[::1]"
port = 50052

[worker]
concurrency = 10
poll_interval = 1000

[health]
scheme = "http"
host = "0.0.0.0"
port = 8082

[rabbitmq]
host = "localhost"
port = 5672
username = "cipher"
password = "cipher"
